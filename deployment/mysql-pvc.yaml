apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
  namespace: beta
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: database
  resources:
    requests:
      storage: 1Gi